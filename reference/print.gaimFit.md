# Printing a `gaimFit` object

The default print method for a `gaimFit` object.

## Usage

``` r
# S3 method for class 'gaimFit'
print(x, ...)
```

## Arguments

- x:

  A model object of class `gaimFit` as produced by
  [`model_gaim()`](https://nuwani-palihawadana.github.io/smimodel/reference/model_gaim.md)
  function.

- ...:

  Other arguments not currently used.
