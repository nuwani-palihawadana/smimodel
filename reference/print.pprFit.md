# Printing a `pprFit` object

The default print method for a `pprFit` object.

## Usage

``` r
# S3 method for class 'pprFit'
print(x, ...)
```

## Arguments

- x:

  A model object of class `pprFit` as produced by
  [`model_ppr()`](https://nuwani-palihawadana.github.io/smimodel/reference/model_ppr.md)
  function.

- ...:

  Other arguments not currently used.
