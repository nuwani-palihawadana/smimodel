# Printing a `smimodel` object

The default print method for a `smimodel` object.

## Usage

``` r
# S3 method for class 'smimodel'
print(x, ...)
```

## Arguments

- x:

  An object of class `smimodel`.

- ...:

  Other arguments not currently used.
