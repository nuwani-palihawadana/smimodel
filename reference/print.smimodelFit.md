# Printing a `smimodelFit` object

The default print method for a `smimodelFit` object.

## Usage

``` r
# S3 method for class 'smimodelFit'
print(x, ...)
```

## Arguments

- x:

  An object of class `smimodelFit`.

- ...:

  Other arguments not currently used.

## Value

No return value; called for side effects. Prints a summary of the fitted
model to console.
